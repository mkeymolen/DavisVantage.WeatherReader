# version format
version: 1.0.0-alpha.{build}
branches:
  only:
  - master
pull_requests:
  do_not_increment_build_number: true
build_script:
- ps: .\build\build.ps1 $env:APPVEYOR_BUILD_VERSION
test_script:
- dotnet test "test\DavisVantage.WeatherReader.Tests"